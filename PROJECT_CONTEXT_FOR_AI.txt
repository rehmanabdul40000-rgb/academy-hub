ACADEMY HUB — AI PROJECT HANDOFF

Repository: rehmanabdul40000-rgb/academy-hub
Current architecture: browser localStorage only.
Do not reintroduce Supabase/PostgreSQL/scalable cloud data unless the user explicitly requests a new database phase.
Do not modify tuition-tide-pro.

Navigation:
Dashboard; All Students; Male Students; Female Students; Morning Shift; Evening Shift; Add Student; Shift Management; Users & Permissions; Change Password; Settings.

Student fields:
Student ID (manual, required); Full Name (required); Gender (required: Male/Female); Phone optional; Course/Class optional; Date Joined optional; Shift optional/blank allowed; Class Time auto from shift but editable; Total Fees; Amount Paid at Enrollment optional/blank; Remaining Fees calculated; Payment Status automatic; Notes; Saved At.

Shift rules:
- Shift dropdown only Morning Shift and Evening Shift.
- Blank Shift is valid.
- Class Time auto-fills from shift and remains editable.

Features:
- Dashboard with time-based greeting, current time, student stats, fee summary, outstanding fees, recent students, gender summary, quick payment.
- Student search/filter/view/edit/delete.
- Recently Deleted and Restore.
- Male/Female/Morning/Evening sections.
- Collect Payment, payment history, receipts/print, reports.
- Excel export: Students, Fee Summary, Fee Analytics, Payment History, Male Students, Female Students.
- Excel/CSV import.
- Settings for academy/session/currency/contact/address/admin display name/admin username/password.
- Workspace backup/restore.
- Users & Permissions.
- Change Password.
- Workspace section enable/disable.
- Dark theme and charcoal/dark-slate Light theme.
- Quick theme buttons in top header.

Admin display name is used by greeting, profile, receipts and Excel.
Password is hidden by default with show/hide control.
Current permissions are application/UI level, not server-backed multi-user authorization.

Vercel:
- Deploy GitHub repo to Vercel.
- Keep repository root.
- Let Vercel detect React/TanStack/Vite.
- Optional initial password env: VITE_DEFAULT_ADMIN_PASSWORD.
- Existing intended credentials: admin / 123.
- localStorage is NOT shared between localhost and Vercel.

AI rules:
- Preserve all existing features unless explicitly asked to change them.
- Preserve the four segment sections.
- Do not make optional fields required.
- Preserve fee/payment behavior and Excel behavior.
- Preserve theme/readability.
- Test locally before production.
- Never expose secrets.
- Prefer small isolated commits.


## Final Vercel deployment guide

The stable release is browser/localStorage based. It does not use Supabase as its active data store.

### Dashboard deployment
1. Test locally first with `npm run build` and `npm run dev`.
2. Push only tested code to GitHub `main`.
3. Open Vercel and choose **Add New → Project**.
4. Import `rehmanabdul40000-rgb/academy-hub`.
5. Keep the repository root as the Root Directory (`./`).
6. Do not convert the project to Next.js; keep the existing React/TanStack Start setup.
7. If Vercel asks for a build command, use `npm run build`.
8. Deploy and test the generated `*.vercel.app` URL.

Vercel supports Vite/React deployments and GitHub-connected deployments. See the official Vercel Vite/React template: https://vercel.com/templates/template/vite-react

### CLI deployment
```bash
npm run build
npx vercel --prod
```

### Important
- Do not add Supabase service-role/secret keys to Vercel for this stable localStorage release.
- Localhost browser storage and Vercel browser storage are separate. Vercel is the live website, not a shared database.
- If the Vercel project is already connected to this GitHub repository, pushing a tested commit to `main` can trigger a new deployment.


## VS Code diagnostics
- TanStack Router messages saying route exports will not be code-split are informational optimization warnings, not application failures.
- The old local `.github/workflows/fix-student-finance-excel.yml` workflow is not part of the current stable GitHub release. If it exists only in a local checkout and creates hundreds of YAML/embedded-script diagnostics, delete that local file and do not recreate the experimental workflows.
